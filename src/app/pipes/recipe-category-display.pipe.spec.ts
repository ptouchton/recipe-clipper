import { RecipeCategoryDisplayPipe } from './recipe-category-display.pipe';

describe('RecipeCategoryDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeCategoryDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
